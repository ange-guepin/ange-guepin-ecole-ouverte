{
  "name": "11ty-sass",
  "version": "1.0.0",
  "type": "module",
  "description": "11ty starter template with SASS support and Dark Mode.",
  "keywords": [
    "11ty",
    "SASS",
    "SCSS",
    "eleventy"
  ],
  "author": "Nikita Zanella",
  "license": "ISC",
  "scripts": {
    "watch:sass": "sass --load-path=node_modules --watch src/sass:src/css",
    "watch:eleventy": "eleventy --serve",
    "build:sass": "sass --load-path=node_modules src/sass:src/css",
    "build:eleventy": "eleventy",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:sass --parallel build:*"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@picocss/pico": "^1.5.6",
    "luxon": "^3.7.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.83.0"
  }
}
